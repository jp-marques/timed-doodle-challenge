// Vitest + Testing Library global setup
// - Extends expect with jest-dom matchers for better DOM assertions
// - Runs in a jsdom test environment configured in vite.config.ts

import '@testing-library/jest-dom';

